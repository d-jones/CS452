.data # Program at 3.1 		
3128:CLASS_Object: # ClassDecl at 0.0			
.word 0 # ClassDecl at 0.0  	
3132:CLASS_Lib: # ClassDecl at 0.0			
.word CLASS_Object # ClassDecl at 0.0 	 
.word readLine # ClassDecl at 0.0		
.word readInt # ClassDecl at 0.0	  
.word readChar # ClassDecl at 0.0			
.word printStr # ClassDecl at 0.0 	 
.word printBool # ClassDecl at 0.0	 
.word printInt # ClassDecl at 0.0  	
.word intToString # ClassDecl at 0.0 	 
.word intToChar # ClassDecl at 0.0 	 
3168:CLASS_END_Lib: # ClassDecl at 0.0  
3168:CLASS_String: # ClassDecl at 0.0 	
.word CLASS_Object # ClassDecl at 0.0	 
.word concat # ClassDecl at 0.0			
.word substring # ClassDecl at 0.0 	
.word length # ClassDecl at 0.0		
.word charAt # ClassDecl at 0.0	  
.word compareTo # ClassDecl at 0.0	
3192:CLASS_END_String: # ClassDecl at 0.0	
3192:CLASS_RunMain: # ClassDecl at 0.0   
.word CLASS_Object # ClassDecl at 0.0  	
3196:CLASS_END_RunMain: # ClassDecl at 0.0 		
3196:CLASS_Main: # ClassDecl at 3.7	  
.word CLASS_Object # ClassDecl at 3.7	 
.word fcn_30_main # ClassDecl at 3.7 	 
.word fcn_63_diffSquares # ClassDecl at 3.7		 
3208:CLASS_END_Main: # ClassDecl at 3.7	 	
3208:CLASS_END_Object: # ClassDecl at 0.0	 	
 .data # Program at 3.1			
  .byte 32 # StringLiteral at 8.15			
  .byte 0 # StringLiteral at 8.15	 
  .byte 0 # StringLiteral at 8.15	
  .byte 0 # StringLiteral at 8.15	 	
  .word CLASS_String # StringLiteral at 8.15  
  .word 2 # StringLiteral at 8.15  	
  .word -1 # StringLiteral at 8.15		
3224:strLit_15: # StringLiteral at 8.15 		
  .byte 10 # StringLiteral at 10.15			
  .byte 0 # StringLiteral at 10.15  	
  .byte 0 # StringLiteral at 10.15 
  .byte 0 # StringLiteral at 10.15	 	
  .word CLASS_String # StringLiteral at 10.15   
  .word 2 # StringLiteral at 10.15	 
  .word -1 # StringLiteral at 10.15   
3240:strLit_27: # StringLiteral at 10.15   
  # ENTER NODE # Program at 3.1   
  .text # Program at 3.1 		
  .globl main # Program at 3.1	 
0:  main: # Program at 3.1	 
0#  jal vm_init # Program at 3.1 
    # ENTER NODE # ExpStatement at 0.0		
      # ENTER NODE # Call at 0.0 		
        # ENTER NODE # NewObject at 0.0			
4#        li $s6, 1 # NewObject at 0.0  	
8#        li $s7, 0 # NewObject at 0.0		
12#        jal newObject # NewObject at 0.0	  
16#        la $t0, CLASS_Main # NewObject at 0.0 
20#        sw $t0, -12($s7) # NewObject at 0.0			
        # EXIT NODE # NewObject at 0.0		
24#      lw $t0, 0($sp) # Call at 0.0	 
28#      beq $t0, $zero, nullPtrException # Call at 0.0	 	
32#      lw $t0, -12($t0) # Call at 0.0 	
36#      lw $t0, 4($t0) # Call at 0.0 		
40#      jalr $t0 # Call at 0.0   
      # EXIT NODE # Call at 0.0			
    # EXIT NODE # ExpStatement at 0.0		
44#  li $v0, 10 # Program at 3.1 
48#  syscall # Program at 3.1  	
    # ENTER NODE # MethodDeclVoid at 5.17	 	
    .globl fcn_30_main # MethodDeclVoid at 5.17 	
52:    fcn_30_main: # MethodDeclVoid at 5.17 
52#    subu $sp, $sp, 8 # MethodDeclVoid at 5.17 	 
56#    sw $ra, 4($sp) # MethodDeclVoid at 5.17		 
60#    sw $s2, ($sp) # MethodDeclVoid at 5.17 	 
64#    lw $s2, 8($sp) # MethodDeclVoid at 5.17	 	
      # ENTER NODE # Assign at 6.6 	 
        # ENTER NODE # NewObject at 6.8   
68#        li $s6, 1 # NewObject at 6.8 	
72#        li $s7, 0 # NewObject at 6.8		
76#        jal newObject # NewObject at 6.8 		
80#        la $t0, CLASS_Lib # NewObject at 6.8   
84#        sw $t0, -12($s7) # NewObject at 6.8		
        # EXIT NODE # NewObject at 6.8 	 
88#      lw $t0, ($sp) # Assign at 6.6	  
92#      sw $t0, 0($s2) # Assign at 6.6	 
96#      addu $sp, $sp, 4 # Assign at 6.6		 
      # EXIT NODE # Assign at 6.6   
      # ENTER NODE # ExpStatement at 7.6	 
        # ENTER NODE # Call at 7.6  	
          # ENTER NODE # IdentifierExp at 7.2			
100#          lw $t0, 0($s2) # IdentifierExp at 7.2 	 
104#          subu $sp, $sp, 4 # IdentifierExp at 7.2 		
108#          sw $t0, ($sp) # IdentifierExp at 7.2 	
          # EXIT NODE # IdentifierExp at 7.2 		
          # ENTER NODE # Call at 7.15			
            # ENTER NODE # This at 7.15  	
112#            subu $sp, $sp, 4 # This at 7.15 	 
116#            sw $s2, ($sp) # This at 7.15 
            # EXIT NODE # This at 7.15		
            # ENTER NODE # IntegerLiteral at 7.27 	
120#            subu $sp, $sp, 8 # IntegerLiteral at 7.27	  
124#            sw $s5, 4($sp) # IntegerLiteral at 7.27 
128#            li $t0, 5 # IntegerLiteral at 7.27 
132#            sw $t0, ($sp) # IntegerLiteral at 7.27		
            # EXIT NODE # IntegerLiteral at 7.27 		
            # ENTER NODE # IntegerLiteral at 7.30	 
136#            subu $sp, $sp, 8 # IntegerLiteral at 7.30   
140#            sw $s5, 4($sp) # IntegerLiteral at 7.30 	 
144#            li $t0, 12 # IntegerLiteral at 7.30		 
148#            sw $t0, ($sp) # IntegerLiteral at 7.30 		
            # EXIT NODE # IntegerLiteral at 7.30 
            # ENTER NODE # False at 7.34  	
152#            subu $sp, $sp, 4 # False at 7.34  
156#            sw $zero, ($sp) # False at 7.34	 
            # EXIT NODE # False at 7.34  
160#          lw $t0, 20($sp) # Call at 7.15	 
164#          beq $t0, $zero, nullPtrException # Call at 7.15 	 
168#          lw $t0, -12($t0) # Call at 7.15  	
172#          lw $t0, 8($t0) # Call at 7.15 	
176#          jalr $t0 # Call at 7.15 	 
          # EXIT NODE # Call at 7.15			
180#        lw $t0, 8($sp) # Call at 7.6			
184#        beq $t0, $zero, nullPtrException # Call at 7.6	 	
188#        lw $t0, -12($t0) # Call at 7.6 	
192#        lw $t0, 24($t0) # Call at 7.6	
196#        jalr $t0 # Call at 7.6  
        # EXIT NODE # Call at 7.6		 
      # EXIT NODE # ExpStatement at 7.6 	
      # ENTER NODE # ExpStatement at 8.6 	 
        # ENTER NODE # Call at 8.6  	
          # ENTER NODE # IdentifierExp at 8.2  
200#          lw $t0, 0($s2) # IdentifierExp at 8.2   
204#          subu $sp, $sp, 4 # IdentifierExp at 8.2 	
208#          sw $t0, ($sp) # IdentifierExp at 8.2			
          # EXIT NODE # IdentifierExp at 8.2	 	
          # ENTER NODE # StringLiteral at 8.15 	 
212#          subu $sp, $sp, 4 # StringLiteral at 8.15	 
216#          la $t0, strLit_15 # StringLiteral at 8.15  
220#          sw $t0, ($sp) # StringLiteral at 8.15	
          # EXIT NODE # StringLiteral at 8.15   
224#        lw $t0, 4($sp) # Call at 8.6	 	
228#        beq $t0, $zero, nullPtrException # Call at 8.6 	
232#        lw $t0, -12($t0) # Call at 8.6	
236#        lw $t0, 16($t0) # Call at 8.6		 
240#        jalr $t0 # Call at 8.6			
        # EXIT NODE # Call at 8.6		 
      # EXIT NODE # ExpStatement at 8.6	
      # ENTER NODE # ExpStatement at 9.6 	 
        # ENTER NODE # Call at 9.6	 
          # ENTER NODE # IdentifierExp at 9.2	 
244#          lw $t0, 0($s2) # IdentifierExp at 9.2  
248#          subu $sp, $sp, 4 # IdentifierExp at 9.2		 
252#          sw $t0, ($sp) # IdentifierExp at 9.2	  
          # EXIT NODE # IdentifierExp at 9.2   
          # ENTER NODE # Call at 9.15	  
            # ENTER NODE # This at 9.15	  
256#            subu $sp, $sp, 4 # This at 9.15			
260#            sw $s2, ($sp) # This at 9.15	
            # EXIT NODE # This at 9.15 		
            # ENTER NODE # IntegerLiteral at 9.27			
264#            subu $sp, $sp, 8 # IntegerLiteral at 9.27	
268#            sw $s5, 4($sp) # IntegerLiteral at 9.27 	 
272#            li $t0, 8 # IntegerLiteral at 9.27			
276#            sw $t0, ($sp) # IntegerLiteral at 9.27   
            # EXIT NODE # IntegerLiteral at 9.27	  
            # ENTER NODE # IntegerLiteral at 9.30 		
280#            subu $sp, $sp, 8 # IntegerLiteral at 9.30		 
284#            sw $s5, 4($sp) # IntegerLiteral at 9.30 
288#            li $t0, 3 # IntegerLiteral at 9.30  	
292#            sw $t0, ($sp) # IntegerLiteral at 9.30			
            # EXIT NODE # IntegerLiteral at 9.30	 	
            # ENTER NODE # True at 9.33	  
296#            subu $sp, $sp, 4 # True at 9.33 	 
300#            li $t0, 1 # True at 9.33	 
304#            sw $t0, ($sp) # True at 9.33			
            # EXIT NODE # True at 9.33		
308#          lw $t0, 20($sp) # Call at 9.15			
312#          beq $t0, $zero, nullPtrException # Call at 9.15	 	
316#          lw $t0, -12($t0) # Call at 9.15  
320#          lw $t0, 8($t0) # Call at 9.15 
324#          jalr $t0 # Call at 9.15 	
          # EXIT NODE # Call at 9.15	  
328#        lw $t0, 8($sp) # Call at 9.6 	
332#        beq $t0, $zero, nullPtrException # Call at 9.6	
336#        lw $t0, -12($t0) # Call at 9.6	
340#        lw $t0, 24($t0) # Call at 9.6  
344#        jalr $t0 # Call at 9.6   
        # EXIT NODE # Call at 9.6 	
      # EXIT NODE # ExpStatement at 9.6 	
      # ENTER NODE # ExpStatement at 10.6 	 
        # ENTER NODE # Call at 10.6 
          # ENTER NODE # IdentifierExp at 10.2	 
348#          lw $t0, 0($s2) # IdentifierExp at 10.2 	 
352#          subu $sp, $sp, 4 # IdentifierExp at 10.2 	
356#          sw $t0, ($sp) # IdentifierExp at 10.2			
          # EXIT NODE # IdentifierExp at 10.2	 	
          # ENTER NODE # StringLiteral at 10.15 	 
360#          subu $sp, $sp, 4 # StringLiteral at 10.15			
364#          la $t0, strLit_27 # StringLiteral at 10.15  	
368#          sw $t0, ($sp) # StringLiteral at 10.15  
          # EXIT NODE # StringLiteral at 10.15	
372#        lw $t0, 4($sp) # Call at 10.6 		
376#        beq $t0, $zero, nullPtrException # Call at 10.6		 
380#        lw $t0, -12($t0) # Call at 10.6		
384#        lw $t0, 16($t0) # Call at 10.6 
388#        jalr $t0 # Call at 10.6 
        # EXIT NODE # Call at 10.6	  
      # EXIT NODE # ExpStatement at 10.6  	
392#    lw $ra, 4($sp) # MethodDeclVoid at 5.17 		
396#    lw $s2, 0($sp) # MethodDeclVoid at 5.17 		
400#    addu $sp, $sp, 12 # MethodDeclVoid at 5.17	 	
404#    jr $ra # MethodDeclVoid at 5.17  
    # EXIT NODE # MethodDeclVoid at 5.17  	
    # ENTER NODE # MethodDeclNonVoid at 12.16 		
    .globl fcn_63_diffSquares # MethodDeclNonVoid at 12.16 	 
408:    fcn_63_diffSquares: # MethodDeclNonVoid at 12.16			
408#    subu $sp, $sp, 8 # MethodDeclNonVoid at 12.16 		
412#    sw $ra, 4($sp) # MethodDeclNonVoid at 12.16	  
416#    sw $s2, ($sp) # MethodDeclNonVoid at 12.16 		
420#    lw $s2, 28($sp) # MethodDeclNonVoid at 12.16	 	
      # ENTER NODE # LocalVarDecl at 13.6   
        # ENTER NODE # Times at 13.18	 	
          # ENTER NODE # IdentifierExp at 13.17 		
424#          lw $t0, 20($sp) # IdentifierExp at 13.17	 	
428#          subu $sp, $sp, 8 # IdentifierExp at 13.17  	
432#          sw $s5, 4($sp) # IdentifierExp at 13.17	  
436#          sw $t0, ($sp) # IdentifierExp at 13.17		 
          # EXIT NODE # IdentifierExp at 13.17 		
          # ENTER NODE # IdentifierExp at 13.19 	
440#          lw $t0, 28($sp) # IdentifierExp at 13.19		
444#          subu $sp, $sp, 8 # IdentifierExp at 13.19	  
448#          sw $s5, 4($sp) # IdentifierExp at 13.19	 
452#          sw $t0, ($sp) # IdentifierExp at 13.19 		
          # EXIT NODE # IdentifierExp at 13.19		 
456#        lw $t0, ($sp) # Times at 13.18 	
460#        lw $t1, 8($sp) # Times at 13.18 	 
464#        mult $t0, $t1 # Times at 13.18   
468#        mflo $t0 # Times at 13.18 
472#        addu $sp, $sp, 8 # Times at 13.18			
476#        sw $t0, ($sp) # Times at 13.18 		
        # EXIT NODE # Times at 13.18	 
      # EXIT NODE # LocalVarDecl at 13.6	 
      # ENTER NODE # LocalVarDecl at 14.6		 
        # ENTER NODE # Times at 14.18	 
          # ENTER NODE # IdentifierExp at 14.17  
480#          lw $t0, 20($sp) # IdentifierExp at 14.17   
484#          subu $sp, $sp, 8 # IdentifierExp at 14.17 		
488#          sw $s5, 4($sp) # IdentifierExp at 14.17	
492#          sw $t0, ($sp) # IdentifierExp at 14.17  
          # EXIT NODE # IdentifierExp at 14.17  	
          # ENTER NODE # IdentifierExp at 14.19  
496#          lw $t0, 28($sp) # IdentifierExp at 14.19	  
500#          subu $sp, $sp, 8 # IdentifierExp at 14.19	
504#          sw $s5, 4($sp) # IdentifierExp at 14.19		 
508#          sw $t0, ($sp) # IdentifierExp at 14.19		
          # EXIT NODE # IdentifierExp at 14.19	 	
512#        lw $t0, ($sp) # Times at 14.18   
516#        lw $t1, 8($sp) # Times at 14.18 		
520#        mult $t0, $t1 # Times at 14.18			
524#        mflo $t0 # Times at 14.18	 	
528#        addu $sp, $sp, 8 # Times at 14.18	 
532#        sw $t0, ($sp) # Times at 14.18	 	
        # EXIT NODE # Times at 14.18		
      # EXIT NODE # LocalVarDecl at 14.6  
      # ENTER NODE # LocalVarDecl at 15.6 	 
        # ENTER NODE # Minus at 15.23 
          # ENTER NODE # IdentifierExp at 15.15		 
536#          lw $t0, 8($sp) # IdentifierExp at 15.15	 
540#          subu $sp, $sp, 8 # IdentifierExp at 15.15 	
544#          sw $s5, 4($sp) # IdentifierExp at 15.15			
548#          sw $t0, ($sp) # IdentifierExp at 15.15	
          # EXIT NODE # IdentifierExp at 15.15		 
          # ENTER NODE # IdentifierExp at 15.24 	 
552#          lw $t0, 8($sp) # IdentifierExp at 15.24 
556#          subu $sp, $sp, 8 # IdentifierExp at 15.24	  
560#          sw $s5, 4($sp) # IdentifierExp at 15.24 	 
564#          sw $t0, ($sp) # IdentifierExp at 15.24	
          # EXIT NODE # IdentifierExp at 15.24 	
568#        lw $t0, ($sp) # Minus at 15.23	
572#        lw $t1, 8($sp) # Minus at 15.23	
576#        subu $t0, $t1, $t0 # Minus at 15.23		 
580#        addu $sp, $sp, 8 # Minus at 15.23  	
584#        sw $t0, ($sp) # Minus at 15.23  	
          # ENTER NODE # Minus at 15.23	 	
          # EXIT NODE # LocalVarDecl at 15.6	 	
          # ENTER NODE # If at 16.2	 
            # ENTER NODE # IdentifierExp at 16.6	 
588#            lw $t0, 32($sp) # IdentifierExp at 16.6  	
592#            subu $sp, $sp, 4 # IdentifierExp at 16.6 
596#            sw $t0, ($sp) # IdentifierExp at 16.6 		
            # EXIT NODE # IdentifierExp at 16.6  
600#          lw $t0, ($sp) # If at 16.2		
604#          addu $sp, $sp, 4 # If at 16.2 		
608#          beq $t0, $zero, if_else_61 # If at 16.2	  
            # ENTER NODE # Block at 16.15 	
              # ENTER NODE # Assign at 17.13   
                # ENTER NODE # Minus at 17.15	
                  # ENTER NODE # IntegerLiteral at 17.15 	
612#                  subu $sp, $sp, 8 # IntegerLiteral at 17.15	 	
616#                  sw $s5, 4($sp) # IntegerLiteral at 17.15   
620#                  li $t0, 0 # IntegerLiteral at 17.15  	
624#                  sw $t0, ($sp) # IntegerLiteral at 17.15			
                  # EXIT NODE # IntegerLiteral at 17.15 	
                  # ENTER NODE # IdentifierExp at 17.16 		
628#                  lw $t0, 8($sp) # IdentifierExp at 17.16		 
632#                  subu $sp, $sp, 8 # IdentifierExp at 17.16		 
636#                  sw $s5, 4($sp) # IdentifierExp at 17.16		 
640#                  sw $t0, ($sp) # IdentifierExp at 17.16	 	
                  # EXIT NODE # IdentifierExp at 17.16		
644#                lw $t0, ($sp) # Minus at 17.15		 
648#                lw $t1, 8($sp) # Minus at 17.15  	
652#                subu $t0, $t1, $t0 # Minus at 17.15	 	
656#                addu $sp, $sp, 8 # Minus at 17.15   
660#                sw $t0, ($sp) # Minus at 17.15	
                  # ENTER NODE # Minus at 17.15  
664#                  lw $t0, ($sp) # Assign at 17.13  
668#                  sw $t0, 8($sp) # Assign at 17.13		 
672#                  addu $sp, $sp, 8 # Assign at 17.13 		
                  # EXIT NODE # Assign at 17.13  	
                # EXIT NODE # Block at 16.15 	 
676#              j if_done_61 # If at 16.2		
680:              if_else_61: # If at 16.2 	
                # ENTER NODE # Block at 19.2			
                # EXIT NODE # Block at 19.2	
680:              if_done_61: # If at 16.2 	
              # EXIT NODE # If at 16.2		 
              # ENTER NODE # IdentifierExp at 19.9 		
680#              lw $t0, 0($sp) # IdentifierExp at 19.9  
684#              subu $sp, $sp, 8 # IdentifierExp at 19.9			
688#              sw $s5, 4($sp) # IdentifierExp at 19.9 	 
692#              sw $t0, ($sp) # IdentifierExp at 19.9 	
              # EXIT NODE # IdentifierExp at 19.9			
696#            lw $ra, 36($sp) # MethodDeclNonVoid at 12.16		
700#            lw $s2, 32($sp) # MethodDeclNonVoid at 12.16	 
704#            lw $t0, ($sp) # MethodDeclNonVoid at 12.16	  
708#            sw $t0, 56($sp) # MethodDeclNonVoid at 12.16	 	
712#            sw $s5, 60($sp) # MethodDeclNonVoid at 12.16 
716#            addu $sp, $sp, 56 # MethodDeclNonVoid at 12.16 	
720#            jr $ra # MethodDeclNonVoid at 12.16  	
              # ENTER NODE # MethodDeclNonVoid at 12.16	  
              # EXIT NODE # Program at 3.1		
###############################################################
# MiniJava/UP library for MIPS/Spim -- version that assumes
#    one-word boolean on stack
# author: Steven R. Vegdahl
# date: 7-13 July 2004
# modified 12-17 March 2007
# modified 3-25 May 2007
# status: reasonably debugged (allegedly)
###############################################################

	.text

###############################################################
# readLine() - library method (class Lib)
# - reads line from standard input
# - produces String that contains the line read, except that it
#   does not include the end-of-line character-sequence. An
#   end-of-line character-sequence is one of the following:
#   - a return character followed by a newline character
#   - a newline character not preceded by a return character
#   - a return character not followed by a newline character
#   - an end-of-file character that follows at least one data
#     character
# - returns null on end-of-file
# - parameter:
#   - ($sp) - this-pointer
# - return-value:
#   - ($sp) - pointer to string containing line that was read
# - anomalies:
#   - with bare "return", looks ahead one character to check for
#     newline.  This could cause non-intuitive behavior when
#     run interactively.
###############################################################
724:readLine:
	
724#	subu $sp,$sp,8 # allocate space for data tag, saving $ra
728#	sw $ra, 4($sp) # save $ra
732#	move $t1,$sp # save "original" sp

736:  doRead:

	# read the character
736#	jal readLogicalChar

	# if we have a 'return', read another character to check for
	# newline
740#	subu $t2,$v0,13
744#	bne $t2,$zero,notReturnRL
748#	jal readLogicalChar
752#	subu $t2,$v0,10 # check for newline
756#	beq $t2,$zero,foundNewLine
760#	sw $v0,lastCharRead # push back character into queue
764#	j foundNewLine # go process the line

768:  notReturnRL:

	# at this point, $v0 has our character

768#	subu $t0,$v0,10
772#	beq $t0,$zero,foundNewLine
776#	blt $v0,$zero,foundEof
777##...slt $at,$v0,$zero
781##...bne $at,$zero,$+16

	# we have a character, so push it onto stack
784#	subu $sp,$sp,4
788#	sw $v0,($sp)

	# loop back up to get next character
792#	j doRead

796:  foundEof:
	# if we had actually read some characters before hitting
        # the eof, go return them as if a newline had been read
796#	bne $t1,$sp foundNewLine

	# otherwise, we got end of file without having read any
  # new characters, so return null
800#	sw $zero,8($sp) # return-value: null
804#	j rlReturn # return

808:  foundNewLine:

	# at this point, we have our newline (or end-of-file), and all space
        # on the stack above $t1 are characters to be put into the string.
        # That is therefore the number of data words to allocate (plus 1
        # more for the class-pointer)


	# set up GC tag and char-count on stack
808#	subu $t0,$t1,$sp # number of chars we read (times 4)
812#	srl $s6,$t0,2 # number of words on stack with chars
816#	subu $sp,$sp,4 # push char-count ...
820#	sw $s6,($sp) # ... onto stack
824#	addu $t2,$t0,5 # GC tag, incl. for count-word
828#	sw $t2,($t1) # store GC tag
	
	# allocate the appropriate Object
832#	addu $s6,$s6,7 # 3 to round up, plus 1 for v-table pointer word
836#	srl $s6,$s6,2 # data words in object
840#	move $s7,$zero # # object words in object
844#	jal newObject # allocate space
	
	# store header words
848#	la $t0,CLASS_String
852#	sw $t0,-12($s7) # store class tag (String) into object
856#	lw $t2,4($sp) # char-count
860#	sll $t1,$t2,2 # 4 times number of chars
864#	subu $t2,$zero,$t2 # negative of char-count
868#	sw $t2,-4($s7) # store negative char-count as header-word 2

	# set up pointers to various parts of stack and object
872#	lw $t0,-8($s7) # data words in object
876#	sll $t0,$t0,2 # data bytes in object
880#	subu $t0,$s7,$t0 # place to store first character (plus 8)	
884#	subu $t0,$t0,$t2 # place to store last character (plus 9)
888#	addu $sp,$sp,8 # pop redundant object-pointer and count
892#	addu $t1,$t1,$sp # first non-char spot on stack

	# at this point:
	#  $t0 points to the target-spot for the last character (plus 9)
	#  $t1 contains top spot on the stack not containing a char
	#  $sp points to the word with the last source character

	# copy the characters, popping each off the stack

896#	beq $sp,$t1,doneCharCopy
900:  charCopyLoop:
900#	lw $t2,($sp)
904#	sb $t2,-9($t0)
908#	addu $sp,$sp,4
912#	subu $t0,$t0,1
916#	bne $sp,$t1,charCopyLoop
920:  doneCharCopy:

	# put our pointer (the return value) on the eventual top stack
920#	sw $s7,8($sp)
	
924:  rlReturn:
	# restore return address and return
924#	lw $ra,4($sp) # restore return address, ...
928#	addu $sp,$sp,8 # ... by popping it (and this-pointer) off stack
932#	jr $ra

###################################################################
# readInt() - library method (class Lib)
# - skips whitespace
# - then attempts to read a base-10 integer from standard input
# - aborts program if a valid integer is not found
# - returns the integer that is read
# - truncates on overflow
# - parameter:
#   - ($sp) - this-pointer
# - return-value:
#   - ($sp) - value that was read
#   - 4($sp) - (dummy) GC tag
###################################################################
936:readInt:

	# save $ra by pushing onto stack
936#	subu $sp,$sp,4
940#	sw $ra,($sp)

944:  riSkipWhiteLoop:
	# read a character
944#	jal readLogicalChar

	# if character <= 32 ascii, check for whitespace; if not
	# whitespace, abort 
948#	subu $t0,$v0,32
952#	bgt $t0,$zero,nonWhite
953##...slt $at,$zero,$t0
957##...bne $at,$zero,$+44
960#	beq $t0,$zero,riSkipWhiteLoop # start over if space
964#	subu $t0,$v0,10
968#	beq $t0,$zero,riSkipWhiteLoop # start over if newline
972#	subu $t0,$v0,9
976#	beq $t0,$zero,riSkipWhiteLoop # start over if tab
980#	subu $t0,$v0,13
984#	beq $t0,$zero,riSkipWhiteLoop # start over if carriage return
988#	subu $t0,$v0,12
992#	beq $t0,$zero,riSkipWhiteLoop # start over if form-feed
996#	j badIntegerFormat # illegal integer char: abort program

1000:  nonWhite:
1000#	subu $t0,$v0,'-'
1004#	li $t4,1 # final multiplier
1008#	bne $t0,$zero,helpReadInt # go read
1012#	li $t4,-1 # -1 in final multiplier

	# read another character to make up for the '-'
1016#	jal readLogicalChar

1020:  helpReadInt:
1020#	li $t2,10
1024#	subu $t1,$v0,'0' # convert digit to 0-9 value
1028#	bgeu $t1,$t2,badIntegerFormat # abort if not digit
1029##...sltu $at,$t1,$t2
1033##...beq $at,$zero,$+2080
1036#	move $t3,$t1 #

	#### at this point, $t3 contains value of the first digit read,
	#### and $t2 contains the value 10

1040:  digitLoop:
	# loop invariants:
	# - $t3 contains the value of the number we've read so far
	# - $t2 contains the value 10

1040#	jal readLogicalChar # read next character
1044#	subu $t1,$v0,'0' # convert digit to 0-9 value
1048#	bgeu $t1,$t2,doneDigitLoop # abort if not digit
1049##...sltu $at,$t1,$t2
1053##...beq $at,$zero,$+20

1056#	mul $t3,$t3,$t2 # multiply old value by 10
1057##...mult $t3,$t2
1061##...mflo $t3
1064#	addu $t3,$t3,$t1 # add in value of new digit
1068#	j digitLoop

1072:  doneDigitLoop:

	# "push back" unused character into queue
1072#	sw $v0,lastCharRead

	# restore return address and overwrite it with return-val;
	# write dummy GC tag as second word of return-val
1076#	lw $ra,($sp)
1080#	mult $t3,$t4 # multiply to account for poss. minus sign
1084#	mflo $t3
1088#	sw $t3,($sp) # result
1092#	sw $s5,4($sp) # dummy GC tag
	#lw $zero,4($sp)#**"" #--FOR MEMORY TAGGING
	
	# return
1096#	jr $ra

###################################################################
# readChar() - library method (class Lib)
# - reads a single character from standard input
# - returns the integer that is the encoding of the character
# - returns -1 if end of file was encountered
# - parameter:
#   - ($sp) - this-pointer
# - return-value:
#   - ($sp) - value that was read
#   - 4($sp) - (dummy) GC tag
###################################################################
1100:readChar:

	# save $ra by pushing onto stack
1100#	subu $sp,$sp,4
1104#	sw $ra,($sp)

  	# read the character
1108#	jal readLogicalChar

	# restore return address; put value (and dummy GC tag),
	# replacing this-pointer saved return address
1112#	lw $ra,($sp)
1116#	sw $s5,4($sp)
	#lw $zero,4($sp)#**"" #--FOR MEMORY TAGGING
1120#	sw $v0,($sp)

	# return
1124#	jr $ra

########################################################
# printStr(str) - library method (class Lib)
# - prints string to standard output
# parameters:
#   - ($sp) - the string to print
#   - 4($sp) - this-pointer
########################################################
1128:printStr:
	# check for null, printing "(null)", if so
1128#	lw $t0,($sp)
1132#	bne $t0,$zero,psNotNull

	# print "(null)"
1136#	la $a0,nullStr
1140#	li $v0,4
1144#	syscall
1148#	j donePrintStr
	
1152:  psNotNull:
	##### we have a non-null string #####
	# this means that:
	# - number of data words in object is in -8($t0)
	# - negative of number of characters in string is in -4($t0)
	# - string begins at $t0-8-(#dataWords*4), stored 1 char per byte
1152#	subu $t0,$t0,8
1156#	lw $t1,($t0) # word just beyond end of string
1160#	sll $t1,$t1,2
1164#	subu $t1,$t0,$t1 # first word in string
1168#	lw $t0,4($t0) # negative of string-length
1172#	subu $t0,$t1,$t0 # byte just beyond last char in string

	# print the chars in the string
1176#	beq $t0,$t1,donePrintStr
1180:  psLoop:
1180#	lb $a0,($t1) # next byte
1184#	li $v0,11 # code for printing char
1188#        syscall # print the char
1192#	addu $t1,$t1,1 # go to next char
1196#	blt $t1,$t0,psLoop
1197##...slt $at,$t1,$t0
1201##...bne $at,$zero,$-20

1204:  donePrintStr:
	# pop stack and return
1204#	addu $sp,$sp,8
1208#	jr $ra

########################################################
# printInt(n) - library method (class Lib)
# - prints integer in decimal format to standard output
# - parameters:
#   - ($sp) - the integer to print
#   - 4($sp) - (dummy) GC tag
#   - 8($sp) - this-pointer
########################################################
1212:printInt:
	# pop value off stack, along with 'this'
1212#	lw $a0,($sp)
1216#	addu $sp,$sp,12

	# print it
1220#	li $v0,1 # code for print-int
1224#	syscall

	# return
1228#	jr $ra

########################################################
# printBool(n) - library method (class Lib)
# - prints boolean to standard output
# - parameters:
#   - ($sp) - the boolean to print
#   - 4($sp) - this-pointer
########################################################
1232:printBool:
	# pop value off stack, along with 'this'
1232#	lw $t0,($sp)
1236#	addu $sp,$sp,8

	# print either 'true' or 'false', depending on the value
1240#	la $a0,falseString
1244#	beq $t0,$zero,skipPB
1248#	la $a0,trueString
1252:  skipPB:
1252#	li $v0,4 # code for print-string
1256#	syscall

	# return
1260#	jr $ra

########################################################
# str.substring(n, k) - library method (class String)
# takes a substring of a string: Java: str.substring(n, k)
# - parameters:
#   - ($sp) - k: one beyond index of last char in subrange
#   - 4($sp) - (dummy) GC tag
#   - 8($sp) - n: index of first char in subrange
#   - 12($sp) - (dummy) GC tag
#   - 16($sp) - str: string to take substring of
# - return value:
#  - ($sp) - substring
########################################################
1264:substring:

	# save $ra by pushing onto stack
1264#	subu $sp,$sp,4
1268#	sw $ra,($sp)

	# get string value off stack, test for null
1272#	lw $t0,20($sp)
1276#	beq $t0,$zero,nullPtrException

	# get both indices and string length, and ensure that
	#   0 <= n <= k <= length
1280#	lw $t0,-4($t0) # negative of string length
1284#	subu $t0,$zero,$t0 # string length
1288#	lw $t1,4($sp) # k
1292#	lw $t2,12($sp) # n
1296#	bgt $zero,$t2,strIndexOutOfBounds
1297##...slt $at,$t2,$zero
1301##...bne $at,$zero,$+1780
1304#	bgt $t2,$t1,strIndexOutOfBounds
1305##...slt $at,$t1,$t2
1309##...bne $at,$zero,$+1772
1312#	bgt $t1,$t0,strIndexOutOfBounds
1313##...slt $at,$t0,$t1
1317##...bne $at,$zero,$+1764

	# allocate memory
1320#	subu $s6,$t1,$t2 # # chars in target-string
1324#	addu $s6,$s6,7 # account for extra "class" (4) word + 3 to round up
1328#	srl $s6,$s6,2 # convert bytes-count to word-count
1332#	move $s7,$zero # (no object-bytes in string)
1336#	jal newObject

	# store "String" tag in object-type field
1340#	la $t0,CLASS_String
1344#	sw $t0,-12($s7)
	
	# store negative of count (=n-k) into object-length header-word
1348#	lw $t1,8($sp) # k
1352#	lw $t2,16($sp) # n
1356#	subu $t0,$t2,$t1 # value to store
1360#	sw $t0,-4($s7) # store value
	
	# store result in return-spot on stack
1364#	lw $t3,24($sp) # source string pointer
1368#	sw $s7,24($sp) # store final result in return-spot on stack
	
	# skip byte-copy loop if length is zero
1372#	beq $zero,$t0,doneSubCopyzz

	# get pointers set up in preparation for copy
1376#	lw $t4,-8($t3) # # data words in source string
1380#	sll $t4,$t4,2 # # data bytes in source string
1384#	subu $t3,$t3,$t4 # addr. of first data word of source string (+8)
1388#	addu $t3,$t3,$t2 # addr. of first source data byte to be copied (+8)
1392#	subu $t1,$t3,$t0 # addr. beyond last source data byte to be copied (+8)
1396#	lw $t2,-8($s7) # # data words in target string
1400#	sll $t2,$t2,2 # # data bytes in target string
1404#	subu $t2,$s7,$t2 # addr. of first target data byte (+8)
	
	############################################
	# at this point:
	# - we know that the string has a positive length
	# - $t3 contains 8 + address of the first source-byte
	# - $t1 contains 8 + limit-address of the first source-byte
	# - $t2 contains 8 + address first target byte
	############################################

	# copy the bytes from source to target
1408:  subCopyLoopzz:
1408#	lb $t4,-8($t3)
1412#	sb $t4,-8($t2)
1416#	addu $t2,$t2,1
1420#	addu $t3,$t3,1
1424#	blt $t3,$t1,subCopyLoopzz
1425##...slt $at,$t3,$t1
1429##...bne $at,$zero,$-20
	
1432:  doneSubCopyzz:
	# restore return address, store return value, pop stack
1432#	lw $ra,4($sp) # restore $ra
1436#	addu $sp,$sp,24 # pop stack
	
	# return
1440#	jr $ra

########################################################
# length() - library method (class String)
# returns length of a string: Java: str.length()
# - parameters:
#   - ($sp) - the string
# - return-value:
#   - ($sp) - length of string
#   - 4($sp) - (dummy) GC tag
########################################################
1444:length:

	# get string pointer
1444#	lw $t0,($sp)
	
	# grow stack
1448#	subu $sp,$sp,4

	# store GC tag
1452#	sw $s5,4($sp)
	#lw $zero,4($sp)#**"" #--FOR MEMORY TAGGING

	# push length onto stack
1456#	lw $t0,-4($t0) # -length
1460#	subu $t0,$zero,$t0
1464#	sw $t0,($sp) #store length

	# return
1468#	jr $ra


########################################################
# str1.concat(str2) - library method (class String)
# (as in Java)
# - parameters:
#   - ($sp) - the second string
#   - 4($sp) - the first string
# - returns:
#   - ($sp) - pointer to concatenated string
########################################################
1472:concat:
 	# save $ra by pushing onto stack
1472#	subu $sp,$sp,4
1476#	sw $ra($sp)

	# get string pointers and check parameter for null
1480#	lw $t0,4($sp)
1484#	beq $t0,$zero,nullPtrException
1488#	lw $t1,8($sp)
	
	# get lengths of two strings; allocate object whose size
	# is their sum divided by 4 (rounded up) plus 1
1492#	lw $t0,-4($t0) # negative size of second object
1496#	lw $t1,-4($t1) # negative size of first object
1500#	addu $s6,$t0,$t1 # sum of negative sizes
1504#	sra $s6,$s6,2 # negative word-size of char part
1508#	subu $s6,$zero,$s6 # word size of char part
1512#	addu $s6,$s6,1 # data word size, including v-table word
1516#	move $s7,$zero
1520#	jal newObject

	# store "String" tag in object-type field
1524#	la $t0,CLASS_String
1528#	sw $t0,-12($s7)
	
	# pop rtnVal, $ra and both parameters off stack; push rtnVal
	# onto stack
1532#	lw $ra,4($sp) # return address
1536#	lw $t0,8($sp) # second object
1540#	lw $t1,12($sp) # first object
1544#	addu $sp,$sp,12 # pop
1548#	sw $s7,($sp) # store return value
	
	# get negative sizes; sum and store in new object size-field
1552#	lw $t2,-4($t0) # negative length of second object
1556#	lw $t3,-4($t1) # negative length of first object
1560#	addu $t4,$t2,$t3 # sum of negative lengths
1564#	sw $t4,-4($s7) # store sum as negated target-string length

	#########################################################
	# at this point:
	# - $t0 is pointer to second object
	# - $t1 is pointer to first object
	# - $t2 is negated length of second object
	# - $t3 is negated length of first object
	# - $s7 is pointer to new object
	#########################################################

	# compute addresses for moving data from first string
1568#	lw $t4,-8($t1) # # data words in first string
1572#	sll $t4,$t4,2 # # data bytes in first string
1576#	subu $t1,$t1,$t4 # addr. (+8) of first byte in first string
1580#	lw $t4,-8($s7) # # data words in new string
1584#	sll $t4,$t4,2 # # data bytes in new string
1588#	subu $s7,$s7,$t4 # addr. (+8) of first byte in new string
1592#	beq $zero,$t3,doneConcatLoop1zz # skip first loop is no bytes to copy
1596#	subu $t3,$t1,$t3 # limit (+8) address for first string

	#########################################################
	# at this point:
	# - $t0 is pointer to second object
	# - $t1 is address (+8) of first byte in first object
	# - $t2 is negated length of second object
	# - $t3 is limit-address (+8) of data in first object
	# - $s7 is address (+8) of first byte in new object
	# - note: if data-length of first object is zero, then
	#   we skip over this part, and go to 'doneConcatLoop1'
	#########################################################

	# copy the bytes from first source to target
1600:  concatLoop1zz:
1600#	lb $t4,-8($t1)
1604#	sb $t4,-8($s7)
1608#	addu $s7,$s7,1
1612#	addu $t1,$t1,1
1616#	blt $t1,$t3,concatLoop1zz
1617##...slt $at,$t1,$t3
1621##...bne $at,$zero,$-20
1624:  doneConcatLoop1zz:
  
    # if second string is empty, skip rest of copy
1624#    beq $zero,$t2,doneConcatLoop2zz

  	# compute addresses for moving data from second string
1628#	lw $t4,-8($t0) # # data words in second string
1632#	sll $t4,$t4,2 # # data bytes in second string
1636#	subu $t1,$t0,$t4 # addr. (+8) of first byte in second string
1640#	subu $t3,$t1,$t2 # limit (+8) address for second string

	#########################################################
	# at this point:
	# - $t1 is address (+8) of first byte in second object
	# - $t3 is limit-address (+8) of data in second object
	# - $s7 is address (+8) of next byte to write new object
	# - note: if data-length of second object is zero, then
	#   we skip over this part, and go to 'doneConcatLoop2'
	#########################################################

	# copy the bytes from first source to target
1644:  concatLoop2zz:
1644#	lb $t4,-8($t1)
1648#	sb $t4,-8($s7)
1652#	addu $s7,$s7,1
1656#	addu $t1,$t1,1
1660#	blt $t1,$t3,concatLoop2zz
1661##...slt $at,$t1,$t3
1665##...bne $at,$zero,$-20
1668:  doneConcatLoop2zz:
 
1668:  concatRtnzz:
	# return
1668#	jr $ra

########################################################
# str.charAt(n) - library method (class String)
# accesses a character in a string, as in Java
# - parameters:
#   - ($sp) - the index, n
#   - 4($sp) - dummy GC tag
#   - 8($sp) - the string, str
# - returns:
#   - ($sp) - the character found
#   - 4($sp) - the dummy GC tag
########################################################
1672:charAt:
	# get string
1672#	lw $t0,8($sp)

	# check that index is in bounds
1676#	lw $t1,-4($t0) # negative of # data words in string
1680#	subu $t3,$zero,$t1 # # chars in string
1684#	lw $t2,($sp) # index
1688#	bgeu $t2,$t3,strIndexOutOfBounds
1689##...sltu $at,$t2,$t3
1693##...beq $at,$zero,$+1388

	# access element
1696#	lw $t1,-8($t0) # # data words in object
1700#	sll $t1,$t1,2 # - byte-offset from end of chars
1704#	subu $t1,$t2,$t1 # - address of first char in string, offset by 8
1708#	addu $t0,$t0,$t1 # - address of our char, offset by 8
1712#	lb $t0,-8($t0) # our char

	# pop elements off stack, pushing rtnVal
1716#	addu $sp,$sp,4
1720#	sw $t0,($sp)
1724#	sw $s5,4($sp)
	#lw $zero,4($sp)#**"" #--FOR MEMORY TAGGING

	# return
1728#	jr $ra

########################################################
# intToString(n) - library method (class Lib)
# converts int to string: Java: ""+n
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - dummy GC tag
#   - 8($sp) - this-pointer
# - returns:
#   - ($sp) - the string, which is the string representation of
#     the integer
########################################################
1732:intToString:

	# save return address on stack; allocate space for dummy GC tag
1732#	subu $sp,$sp,8
1736#	sw $ra,4($sp)

	# save current sp
1740#	move $t0,$sp

	# move constant 10 into $t3
1744#	li $t3,10

	# get argument, negate if negative
1748#	lw $t1,8($sp)
1752#	bge $t1,$zero,itsNonNegLoop
1753##...slt $at,$t1,$zero
1757##...beq $at,$zero,$+8
1760#	subu $t1,$zero,$t1

	# loop through, computing unsigned remainder by 10, and
	# storing digits on stack until we reach 0
1764:  itsNonNegLoop:
1764#	divu $t1,$t3
1768#	mflo $t1 # quotient
1772#	mfhi $t4 # remainder
1776#	addu $t4,$t4,'0' # turn remainder into digit
1780#	subu $sp,$sp,4
1784#	sw $t4,($sp) # push digit onto stack
1788#	bne $t1,$zero,itsNonNegLoop

	# push '-' if negative
1792#	lw $t4,8($t0)
1796#	bge $t4,$zero,itsSkipNeg
1797##...slt $at,$t4,$zero
1801##...beq $at,$zero,$+16
1804#	li $t4,'-'
1808#	subu $sp,$sp,4
1812#	sw $t4,($sp)
1816:  itsSkipNeg:

	################################################
	# At this point, all of our digits have been pushed
	# onto the stack.  $sp points to the first one;
	# $t0 contains the limit-pointer (into which we need to
	# write a GC tag).
	################################################

	# compute number of characters on stack (one word per character);
	# write GC tag onto stack; push char-count onto stack
1816#	subu $s6,$t0,$sp
1820#	addu $t3,$s6,5 # GC tag (including for count-word, about to be pushed
1824#	sw $t3,($t0)
1828#	srl $s6,$s6,2
1832#	subu $sp,$sp,4
1836#	sw $s6,($sp)
	
	# allocate memory
1840#	addu $s6,$s6,7 # 3 to round up, plus 4 for vtable word
1844#	srl $s6,$s6,2
1848#	move $s7,$zero # no "object" words in object
1852#	jal newObject

	# restore char-count; pop it and return value from 'newObject'
1856#	lw $t0,4($sp)
1860#	addu $sp,$sp,8

	# store "String" tag into class field
1864#	subu $s7,$s7,8 # address of header-1 word
1868#	la $t1,CLASS_String
1872#	sw $t1,-4($s7)
	
	# store negative of char-count into header-2 word
1876#	subu $t0,$zero,$t0
1880#	sw $t0,4($s7)

1884#	lw $t1,($s7) # number of data words in string
1888#	sll $t1,$t1,2 # number data bytes in string
1892#	subu $t1,$s7,$t1 # first location to store chars in string
1896#	subu $t0,$t1,$t0 # limit address for chars in string

	####################################################
	# at this point:
	# - $sp contains first source character address
	# - $t1 contains first target character address
	# - $t0 contains target-limit address
	####################################################

	# loop through and copy all elements as we pop them off the stack.
	# (In this case, we know that there is it least one.)
1900:  itsLoop:
1900#	lw $t2,($sp)
1904#	addu $sp,$sp,4
1908#	sb $t2,($t1)
1912#	addu $t1,$t1,1
1916#	bne $t1,$t0,itsLoop

	####################################################
	# At this point
	# - ($t0+15)&0xfffffffe is our return value
    # - ($sp) contains garbage (old GC tag)
	# - 4($sp) contains return address
	####################################################

	# adjust stack, restore return address; return
1920#	lw $ra,4($sp)
1924#	addu $sp,$sp,16
1928#	addu $t0,$t0,15
1932#	and $t0,$t0,0xfffffffc
1933##...lui $at,65535
1937##...ori $at,$at,0xfffc
1941##...and $t0,$t0,$at
1944#	sw $t0,($sp)
1948#	jr $ra

########################################################
# intToChar(n) - library method (class Lib)
# converts int to a one-character string: Java: ""+(char)(n&0xff)
# - parameters:
#   - ($sp) - the value to convert, n
#   - 4($sp) - dummy GC tag
#   - 8($sp) - this-pointer
# - returns:
#   - ($sp) - the string, which is the converted character
# - note: only the low 8 bits of the value n are used
########################################################
1952:intToChar:
	# save return address
1952#	subu $sp,$sp,4
1956#	sw $ra,($sp)

	# allocate object
1960#	li $s6,2
1964#	move $s7,$zero
1968#	jal newObject

	# restore $ra, get 'n', popping then and 'newObject' rtnVal
	# off stack
1972#	lw $ra,4($sp)
1976#	lw $t1,8($sp)
1980#	addu $sp,$sp,16

	# store "String" tag into class field
1984#	la $t0,CLASS_String
1988#	sw $t0,-12($s7)

	# store data in string
1992#	sb $t1,-16($s7)
	
	# store negative of size in header-word 2
1996#	li $t0,-1
2000#	sw $t0,-4($s7)

	# store string pointer (return val) on stack for return
2004#	sw $s7,($sp)

	# return
2008#	jr $ra

########################################################
# str1.compareTo(str2) - library method (class String)
# compares two strings as in Java
# - parameters:
#   - ($sp) - second string
#   - 4($sp) - first string
# - returns:
#   - ($sp) - -1, 0, or 1 depending on whether str1 is
#       lexographically less than, equal to or greater than str2
#   - 4($sp) - (dummy) GC tag
########################################################
2012:compareTo:
	# get string pointers and check parameter for null
2012#	lw $t0,($sp) # second string
2016#	beq $t0,$zero,nullPtrException
2020#	lw $t1,4($sp) # first string
	
	# get (negatives of) respective byte-lengths
2024#	lw $t2,-4($t0) # negative length of second string
2028#	lw $t3,-4($t1) # negative length of first string

	# put tentative return value in $t5.
	# The tentative return value is the one that we will use if we get
	# to the end of the shorter string during our comparison-loop.
2032#	slt $t4,$t3,$t2
2036#	slt $t5,$t2,$t3
2040#	subu $t5,$t5,$t4
	
	# at this point:
	# - $t0 contains the pointer to the second string object
	# - $t1 contains the pointer to the first string object
	# - $t5 contains the value to return if the strings compare equal up
	#   to the length of the shortest word

	# get begin-of-string addresses
2044#	lw $t2,-8($t0) # # data words in second string
2048#	lw $t3,-8($t1) # # data words in first string
2052#	sll $t2,$t2,2 # byte-offset to beginning of str2 (+8)
2056#	sll $t3,$t3,2 # byte-offset to beginning of str1 (+8)
2060#	subu $t0,$t0,$t2 # beginning of str1 address (+8)
2064#	subu $t1,$t1,$t3 # beginning of str2 address (+8)
	
	# put $t1-limit into $t2
2068#	beq $zero,$t5,skipStrCmpLenzz
2072#	move $t2,$t3
2076:  skipStrCmpLenzz:
2076#    add $t2,$t1,$t2
	
	# at this point:
	# - $t0 contains 8 plus the address of the first data-byte of str2
	# - $t1 contains 8 plus the address of the first data-byte of str1
	# - $t2 contains 8 plus the address of the last data-type of str1
	# - $t5 contains the value to return if the strings compare equal up
	#   to the length of the shortest word

	#######################################################
	# at this point, we have
	# - $t5 containing the tentative return-value
	# - $t1 containing address of first char in str2
	# - $t2 containing limit for $t1
	# - $t0 containing address of first char in str1
	#######################################################

	# loop through, until we find unequal words or we hit
	# our limit	
2080:  cmpLoopzz:
2080#	lw $t3,-8($t1) # word from str2
2084#	lw $t4,-8($t0) # word from str1
2088#	bne $t3,$t4,cmpNotEqualzz
2092#	addu $t1,$t1,4
2096#	addu $t0,$t0,4
2100#	bne $t1,$t2,cmpLoopzz

	# # got to the end of one string: go set up return
2104#	j cmpHitLimitzz

2108:  cmpNotEqualzz:
	# found unequal characters: return -1 or 1, depending on which is
	# greater
2108#	slt $t5,$t4,$t3 # 1 if str2 > str1, else 0
2112#	sll $t5,$t5,1 # 2 if str2 > str1, else 0
2116#	subu $t5,$t5,1 # 1 if str2 > str1, else -1
2120:  cmpHitLimitzz:
2120#	sw $t5,($sp) # store value
2124#	sw $s5,4($sp) # GC tag
	#lw $zero,4($sp)#**"" #--FOR MEMORY TAGGING
2128#	jr $ra # return

########################################################
# readLogicalChar (millicode)
#  - logically reads a character from standard input
#  - first checks character buffer, consuming it if possible
#  - return-result:
#    - returns character in $v0
#  - side-effects:
#    - reads a character
#    - clobbers $t0
########################################################
2132:readLogicalChar:

	# check if we already have a character
2132#	lw $v0,lastCharRead
2136#	li $t0,-2
2140#	beq $t0,$v0 doReadCh

	# we have our character from the buffer. Wipe out
	# buffer and return
2144#	sw $t0,lastCharRead # store -2 into buffer (i.e. "empty")
2148#	jr $ra # return
	
2152:  doReadCh:
	# we need to actually read a character: read and return
2152#	li $v0,12 # use system call to read a character
2156#	syscall
2160#	jr $ra # return

########################################################
# newObject (millicode)
#  - allocates a new object on the heap
#  - two-word header is set up properly
#  - all non-header words in object are set to zero
#  - parameters:
#    - $s6 = first header-word, which is -1 if it is a data-array
#      allocation, and is the number of data words in the object
#      otherwise
#    - $s7 - second header-word, which is the number of object
#      words in the object (unless $s6 is -1, in which case it
#      is the number of data words in the object
#    - it is illegal for $s6 to be less than -1 (this is not checked)
#    - it is illegal for $s7 to be less than 0 (this is checked)
#  - return-result:
#    - pushed onto the top of the stack
#    - also returned in $s7
#  - side-effects:
#  - may trash all $tx registers and all $sx registers, except the
#    "permanent" ones, which get updated with values that are
#    consistent with the new space
########################################################
2164:newObject:
	# $s6 = # data words (or -1 if data-array allocation)
	#   - note: it is illegal for $s6 to be less than -1
	# $s7 = # object words (# data words if data-array allocation)
	# $ra = return address
	## on return, pointer to new memory is on
	##   top of stack, and also in $s7
	
	# abort if the object size is negative (this would be an array
	# allocation)
2164#	blt $s7,$zero arraySizeOutOfBounds
2165##...slt $at,$s7,$zero
2169##...bne $at,$zero,$+928

	# mark the fact that we have not yet GC'd for this allocation
2172#	move $t5,$zero

	###### TEMPORARY #######
	# for now, go a GC unconditionally, so that a full GC occurs
	# every time we allocate an object
	### let's not do that for now
	# j doGC

2176:  gcJoin:
	# Determine actual size of "before-header" portion.
	# If negative, it really means zero
2176#	move $t3,$s6
2180#	bge $t3,$zero,newSkip
2181##...slt $at,$t3,$zero
2185##...beq $at,$zero,$+8
2188#	move $t3,$zero

2192:  newSkip:

	# at this point:
	#  $s6 contains the first header word
	#  $s7 contains the second header word, which is also the
	#    after-header word count
	#  $t3 contains the before-header word count
	#  $t5 is zero iff we have not GC'd for this allocation

	# determine if we have enough memory available
2192#	addu $t0,$t3,$s7
2196#	sll $t0,$t0,2
2200#	addu $t0,$t0,8 # amount of memory needed
2204#	addu $t1,$t0,$s3
2208#	bgtu $t1,$s4,doGC
2209##...sltu $at,$s4,$t1
2213##...bne $at,$zero,$+56

	# at this point:
	#  $s3 points to beginning of segment, and
	#  $t1 points just past the end

	# zero out the memory
2216#	move $t2,$s3
2220:  zeroObjLoop:
2220#	sw $zero,($s3)
	#lw $zero,($s3)#**"" #--FOR MEMORY TAGGING	

2224#	addu $s3,4
2228#	bltu $s3,$t1,zeroObjLoop
2229##...sltu $at,$s3,$t1
2233##...bne $at,$zero,$-12

	# at this point:
	#  $s3 has been updated to point to the next free slot,
	#    which is also the point just past our object

	# compute pointer value and set up header-words
2236#	sll $t0,$s7,2    # number of post-header bytes
2240#	subu $t0,$s3,$t0 # pointer that we will return

	# store header-values
2244#	sw $s6,-8($t0)   # first header-word
	#lw $zero,-8($t0)#**"H1" #--FOR MEMORY TAGGING
2248#	sw $s7,-4($t0)   # second header-word
	#lw $zero,-4($t0)#**"H2" #--FOR MEMORY TAGGING
	
	# put return-value into $s7 and also push it onto top of stack
2252#	move $s7,$t0
2256#	subu $sp,$sp,4
2260#	sw $t0,($sp)

2264#	jr $ra
	
2268:  doGC:
	#####################################################
	# We need to do a garbage-collect
	#####################################################
 
         # print that we are doing a GC 
         #la $a0,gcMsg # prints message: "GC!" 
         #li $v0,4 # syscall-code for print-string 
         #syscall 
 	  
	# if we've already done a GC for this allocation, then
	# we are actually out heap-memory: abort program
2268#	bne $t5,$zero,outOfHeapMemory
	  
	# save $s2 (our only rooted register) on the stack
	# so that the garbage collector processes it
2272#	subu $sp,$sp,4
2276#	sw $s2,($sp)

	# set $s3 to the address of the new segment and the
	# end-limit of the new segment, respectively,
	# Also, update cur_seg to refer to the other segment
2280#	lw $t0,cur_seg
2284#	move $t7,$s4
2288#	la $t6,seg1_start
2289##...lui $t6,16
2293##...addiu $t6,$t6,3548
2296#	la $s3,seg0_start
2300#	la $s4,seg0_end
2301##...lui $s4,16
2305##...addiu $s4,$s4,3548
2308#	sw $zero,cur_seg
2312#	bne $t0,$zero,skipGc1
2316#	la $t6,seg0_start
2320#	la $s3,seg1_start
2321##...lui $s3,16
2325##...addiu $s3,$s3,3548
2328#	la $s4,seg1_end
2329##...lui $s4,32
2333##...addiu $s4,$s4,3548
2336#	sw $s5,cur_seg
2340:  skipGc1:
2340#	li $t5,-2
2344#	lw $t0,stack_bottom
2348#	subu $t0,4

	##################################
	# TEMPORARY HACK TO EXERCISE GC
	##################################
	#lw $t1,heapFudge
	#addu $t6,$t6,$t1
	#addu $t1,$t1,4
	#addu $s3,$s3,$t1 # fudge new heap pointer

	###############################################################
	# at this point:
	# - $t6 contains the first address of the source space
	# - $t7 contains the limit address of the source space
	# - $s3 contains the first address of the target space
	# - $s4 contains the limit address of the target space
	# - cur_seg has been updated to refer to the target space
	# - $t0 contains the address of the deepest stack element
	# - $sp contains the address of the top stack element
	# - $s2 is available for use, having been pushed onto the stack
	# - $t5 contains the value -2
	###############################################################


	###### begin outer loop to copy all stack-direct objects ######

2352:  gcStackCopyLoop:
2352#	lw $t1,($t0) # current stack element

	# test if we have a GC tag
2356#	sll $t2,$t1,31 # zero iff low bit was zero
2360#	bne $t2,$zero,gcBump # go skip data values if low bit not zero

	# bump stack-address pointer
2364#	subu $t0,$t0,4

	# if value is out of range (which includes null=0), and is
	# therefore does not refer to an object on the heap, just go
	# loop back and do the next one
2368#	bleu $t1,$t6,gcTestIterDone1
2369##...sltu $at,$t6,$t1
2373##...beq $at,$zero,$+148
2376#	bgtu $t1,$t7,gcTestIterDone1
2377##...sltu $at,$t7,$t1
2381##...bne $at,$zero,$+140

	# if the object has already been moved, update the stack-value
	# via the forwarding pointer
2384#	lw $t2,-8($t1) # possible forwarding tag
2388#	bne $t2,$t5,gcNoForward1 # if not forwarding tag, go copy
2392#	lw $t2,-4($t1) # forwarding pointer: object's new address
2396#	sw $t2,4($t0) # update stack value
2400#	j gcTestIterDone1 # do next iteration

2404:  gcNoForward1:
	#########################################################
	# we actually need to copy the object into the new space
	#########################################################

	# compute the amount of space that is needed
2404#	bge $t2,$zero,gcSkip2
2405##...slt $at,$t2,$zero
2409##...beq $at,$zero,$+8
2412#	move $t2,$zero # set to zero if -1 (number of data words)

2416:  gcSkip2:
2416#	sll $t2,$t2,2
2420#	addu $t2,$t2,8
2424#	subu $t4,$t1,$t2 # address of first word of source
2428#	lw $t3,-4($t1) # number of object words (negative treated as zero)
2432#	bge $t3,$zero,gcH2Neg1
2433##...slt $at,$t3,$zero
2437##...beq $at,$zero,$+8
2440#	move $t3,$zero
2444:  gcH2Neg1:
2444#	sll $t3,$t3,2
2448#	addu $t3,$t3,$t1 # address one word past last word of source

2452#	addu $t2,$s3,$t2 # pointer to target object

	#########################################################
	# At this point:
	# - $t0 contains the address of the stack slot we'll
	#   process next
	# - $t1 contains the contents of the stack slot we're
	#   currently working on, which is a pointer to the source
	#   object (i.e., the address just beyond the object's header
	# - $t2 contains the pointer to the target object
	# - $t3 contains the limit address of the source object
	# - $t4 contains the first address of the source object
	# - $t5 contains the value -2	
	# - $t6 contains the first address of the source space
	# - $t7 contains the limit address of the source space
	# - $s3 contains the first unallocated address of the
	#   target space, which is also the first address of the
	#   target object
	# - $s4 contains the limit address of the target space
	# - $s5 contains the value 5
	# - $s6-7 contain the original parameters to the call to
	#   'newObject'
	# - $sp contains the address of the top stack element
	# - available for use: $s0-2
	#########################################################

	# swap first header word and first data word so that header
    # can be found by "trailing finger" in the target space
2456#	lw $s0,($t4)
2460#	lw $s1,-8($t1)
2464#	sw $s1,($t4)
2468#	sw $s0,-8($t1)
	
	# copy all source bytes to the target
2472:  gCinnerCopy1:
2472#	lw $s0,($t4)
2476#	sw $s0,($s3)
2480#	addu $t4,$t4,4
2484#	addu $s3,$s3,4
2488#	bltu $t4,$t3,gCinnerCopy1
2489##...sltu $at,$t4,$t3
2493##...bne $at,$zero,$-20

	###########################################################
	# All bytes have been copied to the target space.  We still
	# need to:
	# - set up forwarding pointer in source object
	# - update the pointer in the current stack slot
	###########################################################

	# set up the forwarding pointer
2496#	sw $t5,-8($t1) # -2 in first header-slot
2500#	sw $t2,-4($t1) # forwarding pointer in second header-slot

	# update the stack slot with the address in the target space
2504#	sw $t2,4($t0)
	#lw $zero,-8($t2)#**"H1" #--FOR MEMORY TAGGING
	#lw $zero,-4($t2)#**"H2" #--FOR MEMORY TAGGING

	# go handle next stack slot (testing if done)
2508#	j gcTestIterDone1

2512:  gcBump:
	#### we have a GC tag.  Bytes to skip: tag value + 3.
2512#	subu $t0,$t0,$t1
2516#	subu $t0,$t0,3

2520:  gcTestIterDone1:
2520#	bgeu $t0,$sp,gcStackCopyLoop
2521##...sltu $at,$t0,$sp
2525##...beq $at,$zero,$-172

	###### end outer loop to copy all stack-direct objects ######

	#############################################################
	# We have finished processing the stack elements.  Now we need
	# to update elements in the heap itself. This may itself involve
	# moving additional objects
	#############################################################

	#########################################################
	# At this point:
	# - $t5 contains the value -2	
	# - $t6 contains the first address of the source space
	# - $t7 contains the limit address of the source space
	# - $s3 contains the first unallocated address of the
	#   target space
	# - $s4 contains the limit address of the target space
	# - $s5 contains the value 5
	# - $s6-7 contain the original parameters to the call to
	#   'newObject'
	# We want to set things up so that in addition:
	# - $t0 is the "trailing finger", containing the address
	#   of the first slot in target space that we have yet
	#   to process.
	# Then during processing:
	# - $t1 will contain the contents of the heap slot we're
	#   currently working on, which is a pointer to the source
	#   object
	# And when we're actually copying an object:
	# - $t2 will contain pointer to the target object
	# - $t3 will contain the limit address of the source object
	# - $t4 will contain the first address of the source object
	# - $s1 will contain the the limit address for the current
	#   object that tells where the pointers in the object end
	# - available for use: $s0
	##########################################################

	# set $t0 to be at the beginning of target-space
2528#	lw $t1,cur_seg
2532#	la $t0,seg0_start
2536#	beq $t1,$zero,gcSkip4
2540#	la $t0,seg1_start
2541##...lui $t0,16
2545##...addiu $t0,$t0,3548

	##################################
	# TEMPORARY HACK TO EXERCISE GC
	##################################
	#lw $s0,heapFudge
	#addu $s0,$s0,4
	#addu $t0,$t0,$s0
	#sw $s0,heapFudge

2548:  gcSkip4:

	# if there were no objects put into the heap during stack
	# processing, we're done, so go finish up
2548#	bgeu $t0,$s3,gcFinishUp
2549##...sltu $at,$t0,$s3
2553##...beq $at,$zero,$+256

	###### begin outer loop to copy all non-stack-direct objects ######

2556:  gcHeapCopyLoop:
	# check if we have a data array
2556#	lw $t1,($t0) # first header word for current object
2560#	bge $t1,$zero,gcNotDataArray # test for neg. num (actually -1)
2561##...slt $at,$t1,$zero
2565##...beq $at,$zero,$+24

	# We have a -1 header-word, which means this object has no pointers.
	# Skip over it and go loop to do next object.
2568#	lw $t1,4($t0) # data words
2572#	addu $t1,$t1,2 # add in # header words
2576#	sll $t1,$t1,2 # convert to byte-count
2580#	addu $t0,$t0,$t1 # skip over object
2584#	j gcTestIterDone2 # go do next object, if any

2588:  gcNotDataArray:
	# get data count for object; swap header-word with first word
	# of object so that they're back in the right place
2588#	sll $t2,$t1,2 # # data bytes
2592#	addu $t2,$t2,8 # to skip header word
2596#	addu $t2,$t0,$t2 # pointer to new object
2600#	lw $t3,-8($t2) # word to swap
2604#	sw $t1,-8($t2) # store header word
2608#	sw $t3,($t0) # restore first word of object

2612#	lw $t3,-4($t2) # # object words (negative treated as zero)
2616#	bge $t3,$zero,gcH2Neg2
2617##...slt $at,$t3,$zero
2621##...beq $at,$zero,$+8
2624#	move $t3,$zero
2628:  gcH2Neg2:
2628#	sll $t3,$t3,2 # # object bytes
2632#	move $t0,$t2 # address of first pointer in object
2636#	add $s1,$t2,$t3 # limit address for this object

	#####################################################
	# At this point, we have to "translate" all pointers,
	# starting at $t0 to (but not including) $s1
	#####################################################

	# if there are no pointer-slots (i.e., $t0=$s1), skip this
	# part
2640#	beq $t0,$s1,gcTestIterDone2
	
2644:  gcNextPointerInObject:
	# get pointer from object
2644#	lw $t1,($t0)

	# if value is out of range, and is therefore does not refer
	# to an object, just go loop back and do the next one
2648#	bleu $t1,$t6,gcGoNextPointerSlot
2649##...sltu $at,$t6,$t1
2653##...beq $at,$zero,$+136
2656#	bgtu $t1,$t7,gcGoNextPointerSlot
2657##...sltu $at,$t7,$t1
2661##...bne $at,$zero,$+128

	# if the object has already been moved, update the stack-value
	# via the forwarding pointer
2664#	lw $t8,-8($t1) # possible forwarding tag
2668#	bne $t8,$t5,gcNoForward2 # if not forwarding tag, go copy
2672#	lw $t8,-4($t1) # forwarding pointer: object's new address
2676#	sw $t8,($t0) # update pointer in object
2680#	j gcGoNextPointerSlot # do next iteration

2684:  gcNoForward2:
	#########################################################
	# we actually need to copy the object into the new space
	#########################################################

	#########################################################
	# At this point:
	# - $t0 contains the address of the heap-slot we're translating
	# - $t1 will contain the contents of the heap-slot we're
	#   currently working on, which is a pointer to the source
	#   object
	# - $t2 will contains pointer to the object we're in the
	#   middle of translating
	# - $t5 contains the value -2	
	# - $t6 contains the first address of the source space
	# - $t7 contains the limit address of the source space
	# - $s3 contains the first unallocated address of the
	#   target space, which will also be the first address
	#   of the target object
	# - $s4 contains the limit address of the target space
	# - $s5 contains the value 5
	# - $s6-7 contain the original parameters to the call to
	#   'newObject'
	# Then during processing:
	# And when we're actually copying an object:
	# - $t3 will contain the limit address of the source object
	# - $t4 will contain the first address of the source object
	# - $s1 will contain the the limit address for the current
	#   object that tells where the pointers in the object end
	# - $t8 will contain a pointer to the target object
	# - available for use: $s0, $t8, $t9
	##########################################################

	# compute the amount of space that is needed
2684#	bge $t8,$zero,gcSkip5
2685##...slt $at,$t8,$zero
2689##...beq $at,$zero,$+8
2692#	move $t8,$zero # set to zero if -1

2696:  gcSkip5:
2696#	sll $t8,$t8,2
2700#	addu $t8,$t8,8
2704#	subu $t4,$t1,$t8 # address of first word of source
2708#	lw $t3,-4($t1)
2712#	bge $t3,$zero,gcNoNeg
2713##...slt $at,$t3,$zero
2717##...beq $at,$zero,$+8
2720#	move $t3,$zero
2724:  gcNoNeg:
2724#	sll $t3,$t3,2
2728#	addu $t3,$t3,$t1 # address one word past last word of source

2732#	addu $t8,$s3,$t8 # pointer to target object

	#########################################################
	# At this point:
	# - $t0 contains the address of the stack slot we'll
	#   process next
	# - $t1 contains the contents of the stack slot we're
	#   currently working on, which is a pointer to the source
	#   object
	# - $t2 will contains pointer to the object we're in the
	#   middle of translating
	# - $t3 contains the limit address of the source object
	# - $t4 contains the first address of the source object
	# - $t5 contains the value -2	
	# - $t6 contains the first address of the source space
	# - $t7 contains the limit address of the source space
	# - $t8 contains the pointer to the target object
	# - $s1 contains the the limit address for the current
	#   object that tells where the pointers in the object end
	# - $s3 contains the first unallocated address of the
	#   target space, which is also the first address of the
	#   target object
	# - $s4 contains the limit address of the target space
	# - $s5 contains the value 5
	# - $s6-7 contain the original parameters to the call to
	#   'newObject'
	# - $sp contains the address of the top stack element
	# - available for use: $s0, $t9
	#########################################################

	# swap first header word and first data word so that header
        # can be found by "trailing finger"
2736#	lw $s0,($t4)
2740#	lw $t9,-8($t1)
2744#	sw $t9,($t4)
2748#	sw $s0,-8($t1)

	# copy all source bytes to the target
2752:  gCinnerCopy2:
2752#	lw $s0,($t4)
2756#	sw $s0,($s3)
2760#	addu $t4,$t4,4
2764#	addu $s3,$s3,4
2768#	bltu $t4,$t3,gCinnerCopy2
2769##...sltu $at,$t4,$t3
2773##...bne $at,$zero,$-20

	###########################################################
	# All bytes have been copied to the target space.  We still
	# need to:
	# - set up forwarding pointer in source object
	# - update the pointer in the current stack slot
	###########################################################

	# set up the forwarding pointer
2776#	sw $t5,-8($t1) # -2 in first header-slot
2780#	sw $t8,-4($t1) # forwarding pointer in second header-slot

	# update the heap-slot with the address in the target space
2784#	sw $t8,($t0)

2788:  gcGoNextPointerSlot:
	# bump $t0 to next slot in object; if not done, loop back
2788#	addu $t0,$t0,4
2792#	bltu $t0,$s1,gcNextPointerInObject
2793##...sltu $at,$t0,$s1
2797##...bne $at,$zero,$-152

2800:  gcTestIterDone2:
2800#	bltu $t0,$s3,gcHeapCopyLoop
2801##...sltu $at,$t0,$s3
2805##...bne $at,$zero,$-248

	###### end outer loop to copy all non-stack-direct objects ######

2808:  gcFinishUp:
	# restore $s2 to its updated value by popping off stack
2808#	lw $s2,($sp)
2812#	addu $sp,$sp,4

	# mark us as having already GC'd
2816#	move $t5,$s5

	# go try and allocate again
2820#	j gcJoin
	    

########################################################
# vm_init (millicode)
#  - initialzes the virtual machine
#  - values 5 stored in $s5
#  - zero ("this pointer") stored in $s2
#  - heap and heap-limit pointers stored respectively in $s3 and $s4
#  - address of bottom of stack stored in 'stack_bottom' memory
#    location
#  - (note: 'cur_seg' memory location is already set to 0)
########################################################
2824:vm_init:

	# mark bottom of stack
2824#	sw $sp,stack_bottom
2828#"stack pointer"	move $sp,$sp#**"stack pointer"  #--FOR MEMORY TAGGING
	
	# set "this" pointer to be null, for now
2832#	move $s2,$zero
2836#"this pointer"	move $s2,$s2#**"this pointer"  #--FOR MEMORY TAGGING

	#set up the "five" register
2840#	li $s5,5
2844#"constant 5"	move $s5,$s5#**"constant 5"  #--FOR MEMORY TAGGING

2848#	la $s3,seg0_start
2852#"next-avail-heap"	move $s3,$s3#**"next-avail-heap"  #--FOR MEMORY TAGGING
2856#	la $s4,seg0_end
2857##...lui $s4,16
2861##...addiu $s4,$s4,3548
2864#"end-heap"	move $s4,$s4#**"end-heap"  #--FOR MEMORY TAGGING
	
	# return
2868#	jr $ra

########################################################
# divide (millicode)
#  - divides first arg by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
2872:divide:
	# get parameters; abort if divisor zero
2872#	lw $t0,($sp)
2876#	lw $t1,8($sp)
2880#	beq $t0,$zero,divByZeroError
	
	# perform division
2884#	div $t1,$t0
2888#	mflo $t0

	# store result, adjust stack and return
2892#	addu $sp,$sp,8 # adjust stack
2896#	sw $t0,($sp) # store result
2900#	jr $ra

########################################################
# remainder (millicode)
#  - takes remainder first arg divided by second (signed divide)
#  - aborts if divisor is zero
#  - follows calling conventions for library methods
#  - parameters:
#    - ($sp) divisor
#    - 4($sp) (dummy) GC tag
#    - 8($sp) dividend
#    - 12($sp) (dummy) GC tag
#  - return value:
#    - ($sp) result
#    - 4($sp) (dummy) GC tag
########################################################
2904:remainder:
	# get parameters; abort if divisor zero
2904#	lw $t0,($sp)
2908#	lw $t1,8($sp)
2912#	beq $t0,$zero,divByZeroError
	
	# perform division
2916#	div $t1,$t0
2920#	mfhi $t0

	# store result, adjust stack and return
2924#	addu $sp,$sp,8 # adjust stack
2928#	sw $t0,($sp) # store result
2932#	jr $ra
	
########################################################
# checkCast (millicode) - checks that a cast is legal
#  - aborts if null
#  - aborts if cast is illegal cast
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) object to check (now checked)
#  - side-effects: clobbers $t2 and $t3
########################################################
2936:checkCast:
	# get object, allow immediately if null
2936#	lw $t2,($sp)
2940#	beq $t2,$zero,checkCastReturn
	
	# get vtable address of object (using Object vtable
	# address for arrays)
2944#	lw $t3,-8($t2) # <= 0 if array
2948#	lw $t2,-12($t2) # vtable address (unless array)
2952#	bgt $t3,$zero,skipArrayCast
2953##...slt $at,$zero,$t3
2957##...bne $at,$zero,$+8
2960#	la $t2,CLASS_Object
	
	# get vtable address of object, abort if less than
	# lower limit or greater then or equal to higher
	# limit
2964:  skipArrayCast:
2964#	bge $t2,$t1,castException
2965##...slt $at,$t2,$t1
2969##...beq $at,$zero,$+152
2972#	blt $t2,$t0,castException
2973##...slt $at,$t2,$t0
2977##...bne $at,$zero,$+144
	
	# return, leaving object unchanged on stack
2980:  checkCastReturn:	
2980#	jr $ra	
	
########################################################
# instanceOf (millicode) - tests whether an object is
#   a member of a given class (or subclass)
#  - returns false if object is null
#  - parameters:
#    - ($sp) object to check
#    - $t0 address of vtable for target-class
#    - $t1 address one past vtable address of last
#      subclass of target-class
#  - return value:
#    - ($sp) true (1) or false (0), depending on whether
#      object is a member
#  - side-effects: clobbers $t2 and $t3
########################################################
2984:instanceOf:
	# get object, go return false if null
2984#	lw $t2,($sp)
2988#	beq $t2,$zero,doneInstanceOf
	
	# get vtable address of object.  If it's an array
	# (which would be H1 <= 0), use vtable address for Object
2992#	lw $t3,-8($t2) # <= 0 if array
2996#	lw $t2,-12($t2) # vtable address (unless array)
3000#	bgt $t3,$zero,skipArrayInstanceOf
3001##...slt $at,$zero,$t3
3005##...bne $at,$zero,$+8
3008#	la $t2,CLASS_Object
	
	# get vtable address of object, abort if less than
	# lower limit or greater then or equal to higher
	# limit
3012:  skipArrayInstanceOf:
3012#	sge $t0,$t2,$t0
3013##...slt $t0,$t2,$t0
3017##...xori $t0,$t0,0x1
3020#	slt $t1,$t2,$t1
	
	# store the AND of the two conditions onto the stack; return
3024#	and $t2,$t0,$t1
3028:  doneInstanceOf: # if we reach here via branch, we know $t2=0
3028#	sw $t2,($sp)
3032#	jr $ra

###########################################################
# jump-targets to terminate program:
# - exit: returns normally
# - outOfHeapMemory: prints "out of memory" error and returns
###########################################################

3036:exitError:
	# assumes $a0 has pointer to null-terminated string

	# print the string
3036#	li $v0,4 # syscall-code for print-string
3040#	syscall
3044:exit:
	# print termination message
3044#	li $v0,4
3048#	la $a0,exitString
3052#	syscall

	# terminate execution
3056#	li $v0,10 #syscall-code for "exit"
3060#	syscall

3064:outOfHeapMemory:
3064#	la $a0,heapMemoryMsg
3068#	j exitError
3072:divByZeroError:
3072#	la $a0,divByZeroMsg
3076#	j exitError
3080:strIndexOutOfBounds:
3080#	la $a0,strIndexOutOfBoundsMsg
3084#	j exitError
3088:arrayIndexOutOfBounds:
3088#	la $a0,arrayIndexOutOfBoundsMsg
3092#	j exitError
3096:arraySizeOutOfBounds:
3096#	la $a0,arraySizeOutOfBoundsMsg
3100#	j exitError
3104:nullPtrException:
3104#	la $a0,nullPtrExceptionMsg
3108#	j exitError
3112:badIntegerFormat:
3112#	la $a0,badIntegerFormatMsg
3116#	j exitError
3120:castException:
3120#	la $a0,castExceptionMsg
3124#	j exitError

############## data section ################
	.data
	.align 2

3240:cur_seg:
	.word 0

3244:lastCharRead:
	.word -2 # -2 => no buffered character

3248:heapMemoryMsg:
	.asciiz "ERROR: out of heap memory\n"
3275:divByZeroMsg:
	.asciiz "ERROR: divide by zero\n"
3298:strIndexOutOfBoundsMsg:
	.asciiz "ERROR: string index out of bounds\n"
3333:arrayIndexOutOfBoundsMsg:
	.asciiz "ERROR: array index out of bounds\n"
3367:arraySizeOutOfBoundsMsg:
	.asciiz "ERROR: array size out of bounds\n"
3400:nullPtrExceptionMsg:
	.asciiz "ERROR: null-pointer exception\n"
3431:badIntegerFormatMsg:
	.asciiz "ERROR: attempt to read badly formatted integer\n"
3479:castExceptionMsg:
	.asciiz "ERROR: illegal cast\n"
3500:gcMsg: 
      .asciiz "\nGC!\n"

3506:nullStr:
	.asciiz "null"
3511:trueString:
	.asciiz "true"
3516:falseString:
	.asciiz "false"
3522:exitString:
	.asciiz "Program terminated.\n"
	
	.align 2
3544:stack_bottom:
	.word 0

#heapFudge: # temporary fudge amount to exercise GC
#	.word 0

3548:seg0_start:
	.space 0x100000
1052124:seg0_end:
1052124:seg1_start:
	.space 0x100000
2100700:seg1_end:



